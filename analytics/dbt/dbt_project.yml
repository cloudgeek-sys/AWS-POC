name: gppa_analytics
version: 1.0.0
config-version: 2

profile: gppa_analytics

model-paths: ["models"]
macro-paths: ["macros"]

target-path: target
clean-targets: ["target", "dbt_packages"]

models:
  gppa_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
